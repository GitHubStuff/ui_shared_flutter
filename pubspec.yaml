name: ui_shared
description: "Collection of shared widgets and utilities for Flutter applications."
version: 0.2.0+1
home_page: http://www.icodeforyou.com
publish_to: none # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.3.0 <4.0.0" # % flutter --version => values of "Dart"
  flutter: ">=3.22.1" # % flutter --version => values of "Flutter

dependencies:
  flutter:
    sdk: flutter
  # logger: ^2.2.0 # Uncomment this line to use logger package {better than debugPrint}

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
